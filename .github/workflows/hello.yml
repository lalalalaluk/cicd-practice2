name: Hello GitHub Actions

on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello GitHub Actions!"
      - name: Print date
        run: date
      - name: Print runner info
        run: |
          uname -a
          pwd
